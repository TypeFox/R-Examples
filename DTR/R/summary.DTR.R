###################################################
### code chunk
###################################################

summary.DTR <- function(object, ...) {   
  class(object) <- "summary.DTR"
  return(object)
}