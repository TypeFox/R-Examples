# Function called within Dest.r and Gst.est.r

          Hs.est<-function(Hs,N){
                             out <- (2*N/(2*N-1))*Hs
                             invisible(out)
                                      }
