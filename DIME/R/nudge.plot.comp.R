nudge.plot.comp <-
function(data,obj,new.plot=TRUE,legpos=NULL,xlim=NULL,ylim=NULL,
  xlab=NULL,ylab=NULL,main=NULL,lwd=NULL,...){
  inudge.plot.comp(data,obj,new.plot=new.plot,legpos=legpos, 
    xlim=xlim,ylim=ylim,xlab=xlab, ylab=ylab, main=main, 
    lwd=lwd,...);
}

