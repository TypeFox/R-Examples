mnplots <-
#function(X, B, MCs=numeric(), type='A', Vmse=numeric(), fig=0, corner=c(0,1), setsP=seq(from=1,to=floor(length(X)/2)), setsN=seq(from=floor(length(X)/2)+1, to=length(X)), removedtype='perc', removedval=.Machine$double.eps, Xtype='data', mseCache=numeric(), subplotdim=numeric(), wsets=rep(1,length(X)), GDM=numeric()) {return (NULL);}
function(X, B, type='A', ...) {return (NULL);}