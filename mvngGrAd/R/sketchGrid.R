setGeneric("sketchGrid",
           def = function(
             i,
             j,
             rowLimit,
             colLimit,
             layers,
             shapeCross,
             excludeCenter,
             ...)
           {
             standardGeneric("sketchGrid")
           }
           )
