"boa.sortiter" <-
function(link)
#-------------------------------------------------------------------------------
#-------------------------------------------------------------------------------
{
   idx <- order(as.numeric(dimnames(link)[[1]]))

   return(link[idx, , drop = FALSE])
}
