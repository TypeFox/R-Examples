#' An example sample data file of 5000 respondents.
#'
#' A dataset containing the values of y (a variable of interest which is outlier-prone),
#' two auxiliary variables x1 and x2,
#' piwt (inverse probability of selection weight), and
#' gregwt (generalized regression estimation weight, assumed to have been
#'  calculated using x1 and x2)
"survdat.example"
